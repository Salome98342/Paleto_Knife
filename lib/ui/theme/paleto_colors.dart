import 'package:flutter/material.dart';

class PaletoColors {
  const PaletoColors._();

  // Backgrounds
  static const bgDeep = Color(0xFF0D0D0D);
  static const bgPanel = Color(0xFF1A1209);
  static const bgPanelAlt = Color(0xFF0F1A0D);

  // NES-style panel borders
  static const borderLight = Color(0xFFE8C97A);
  static const borderMid = Color(0xFF8B6914);
  static const borderDark = Color(0xFF3D2B05);

  // Buttons
  static const btnPrimary = Color(0xFFD4380D);
  static const btnPrimaryLt = Color(0xFFFF6B3D);
  static const btnPrimaryDk = Color(0xFF7A1A00);

  static const btnGacha = Color(0xFFFFB800);
  static const btnGachaLt = Color(0xFFFFE066);
  static const btnGachaDk = Color(0xFF7A5500);

  static const btnSecondary = Color(0xFF2A4A2A);
  static const btnSecondaryLt = Color(0xFF4A8A4A);
  static const btnSecondaryDk = Color(0xFF0D1F0D);

  static const btnNeutral = Color(0xFF2D2D2D);
  static const btnNeutralLt = Color(0xFF4A4A4A);
  static const btnNeutralDk = Color(0xFF111111);

  // Rarity
  static const rarityCommon = Color(0xFF888888);
  static const rarityRare = Color(0xFF4A90D9);
  static const rarityEpic = Color(0xFF9B4DCA);
  static const rarityLegendary = Color(0xFFFFB800);

  // Text
  static const textPrimary = Color(0xFFF5E6C8);
  static const textSecondary = Color(0xFF9E8A6A);
  static const textAccent = Color(0xFFFFB800);

  // Elements
  static const elemFire = Color(0xFFFF4500);
  static const elemIce = Color(0xFF00BFFF);
  static const elemPoison = Color(0xFF7CFC00);
  static const elemElec = Color(0xFFFFD700);
  static const elemDark = Color(0xFF9932CC);
}
